#include "main.h"
#include <string.h>
/**
 * _strlen - a function that returns the length of a string
 * @s: string variable to check for its length
 * Return: it returns the length
 */
int _strlen(char *s)
{
	return (strlen(s));
}
