#include "main.h"
#include <string.h>
/**
 * _strcmp - a function that compares two strings
 * @s1: first string to be compared
 * @s2: second string to be comapred
 * Return: it returns integer at success
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
