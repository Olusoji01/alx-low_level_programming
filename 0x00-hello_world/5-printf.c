#include<stdio.h>
/**
 * main - A program that print a string using
 * printf function
 * void: no argument is passed to the function
 * Return: the program return 0 after execution
 */

int main(void)
{
	char str[57] = "with proper grammar, but the outcome is a piece of art,";

	/*Printing the string with printf function*/
	printf("%s\n", str);

	return (0);
}
