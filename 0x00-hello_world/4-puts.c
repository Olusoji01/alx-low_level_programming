#include<stdio.h>
/**
 * main- a program that print string using puts
 * Return: the program return 0 after execution
 * void: no argument is passed to the main function
 */

int main(void)
{
	char str[52] = "Programming is like building a multilingual puzzle";

/* using puts to print the string char array*/
	puts(str);

	return (0);
}
