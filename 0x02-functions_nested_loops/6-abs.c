#include "main.h"
#include <stdlib.h>
/**
 * _abs - a function that computes the absolute value of an integer
 * @i: value to computes its absolute value
 * Return: it return 0 after execution
 */
int _abs(int i)
{ 
	return (abs(i));
}
