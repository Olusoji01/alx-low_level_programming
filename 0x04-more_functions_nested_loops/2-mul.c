#include "main.h"
/**
 * mul - a function that multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: it returns the result
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
